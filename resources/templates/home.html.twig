<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Fulcrum</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            background: #f5f5f5;
        }
        .box {
            text-align: center;
            padding: 40px;
        }
        h1 {
            font-size: 48px;
            font-weight: 800;
            color: #1a1a2e;
            margin-bottom: 8px;
        }
        h1 span { color: #7c6af7; }
        p { color: #888; font-size: 16px; }
        a {
            display: inline-block;
            margin-top: 24px;
            padding: 10px 24px;
            background: #7c6af7;
            color: #fff;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 600;
        }
    </style>
</head>
<body>
<div class="box">
    <h1>Ful<span>crum</span></h1>
    <p>The core is fixed. Plugins are the leverage.</p>
    <a href="/_fulcrum">Admin Panel →</a>
</div>
</body>
</html>