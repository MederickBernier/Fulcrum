# Application
APP_ENV=development
APP_DEBUG=true
APP_KEY=
APP_URL=http://localhost:8080

# PHP
PHP_VERSION=8.3

# Ports
HTTP_PORT=8080

# WSL2 user — run `id -u` and `id -g` in WSL2 terminal
# to get your values if permissions are off
USER_ID=1000
GROUP_ID=1000